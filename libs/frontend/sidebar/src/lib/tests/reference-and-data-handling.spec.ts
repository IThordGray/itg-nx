describe('Sidebar Reference and Data Handling', () => {
  it('should provide a valid sidebar reference (sidebarRef) upon opening', () => {
    // Test implementation
  });

  it('should inject the SIDEBAR_* injection tokens into the componentType', () => {
    // Test implementation
  });

  it('should provide the data and sidebarRef in the context for a templateRef', () => {
    // Test implementation
  });
});