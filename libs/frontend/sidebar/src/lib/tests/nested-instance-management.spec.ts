describe('Nested Sidebar Management', () => {
  it('should open a child sidebar when requested through the sidebarRef', () => {
    // Test implementation
  });

  it('should contain 2 instances when a child sidebar is opened through the sidebarRef', () => {
    // Test implementation
  });

  it('should replace both sidebar instances with one instance when a new sidebar is opened from the service', () => {
    // Test implementation
  });

  it('should replace only the child instance when a new sidebar is opened through the sidebarRef', () => {
    // Test implementation
  });

  it('should align nested sidebars correctly based on the configuration', () => {
    // Test implementation
  });

  it('should close all nested sidebars when the parent sidebar is closed', () => {
    // Test implementation
  });
});