describe('Content Rendering', () => {
  it('should render the componentType within the sidebar', () => {
    // Test implementation
  });

  it('should render the templateRef within the sidebar', () => {
    // Test implementation
  });

  it('should replace the existing componentType with a new one when requested', () => {
    // Test implementation
  });

  it('should replace the existing templateRef with a new one when requested', () => {
    // Test implementation
  });

  it('should render a default header when the config\'s heading property is a string', () => {
    // Test implementation
  });

  it('should render the specified templateRef when the config\'s heading property is a templateRef', () => {
    // Test implementation
  });
});